# Cross Vision F ユーザーガイド

> 鉄骨製品番号の撮影認識・工程登録 Android アプリ

---

## はじめに

Cross Vision F は、鉄骨製品に刻印・記載された製品番号をスマートフォンのカメラで撮影し、OCR（光学文字認識）で自動的に読み取って工程登録を行うためのアプリです。オフライン環境でも登録作業が可能で、ネットワーク復帰時に自動的にサーバーへデータを同期します。

---

## 目次

1. [アプリの起動と初期設定](#1-アプリの起動と初期設定)
2. [ログイン](#2-ログイン)
3. [工事・工程の選択](#3-工事工程の選択)
4. [画像認識（撮影・ギャラリー選択）](#4-画像認識撮影ギャラリー選択)
5. [カメラで撮影する](#5-カメラで撮影する)
6. [認識結果の確認](#6-認識結果の確認)
7. [製品コードの登録](#7-製品コードの登録)
8. [登録履歴の確認・同期](#8-登録履歴の確認同期)
9. [管理画面（サーバー側）](#9-管理画面サーバー側)
10. [よくある質問・トラブルシューティング](#10-よくある質問トラブルシューティング)

---

## 1. アプリの起動と初期設定

### 必要な環境

- Android 7.0（API 24）以上を搭載したスマートフォンまたはタブレット
- カメラの使用許可（初回起動時に許可を求められます）
- サーバー同期を利用する場合は、サーバーと同じ Wi-Fi ネットワークに接続していること

### 初回起動時の権限許可

アプリを初めて起動した際に、カメラの使用許可を求めるダイアログが表示されます。製品番号の撮影にはカメラが必要ですので、**「許可」** をタップしてください。誤って拒否してしまった場合は、端末の「設定」→「アプリ」→「Cross Vision F」→「権限」からカメラの使用を許可できます。

---

## 2. ログイン

アプリを起動すると、最初に **ログイン画面** が表示されます。

### 操作手順

1. **ユーザーID** を入力します
2. **パスワード** を入力します
3. **「ログイン」** ボタンをタップします

### 注意事項

- ユーザーIDとパスワードの両方が入力されていない場合、エラーメッセージが表示されます
- ログイン情報が正しくない場合は「ユーザーIDもしくはパスワードが違います」と表示されますので、入力内容を確認して再度お試しください
- パスワード入力欄の右側にある目のアイコンをタップすると、入力中のパスワードの表示・非表示を切り替えることができます

---

## 3. 工事・工程の選択

ログイン後、**工事・工程選択画面** が表示されます。ここでは、これから登録作業を行う対象の工事と工程を選択します。

### 操作手順

1. **対象区分**（「製品」または「部品」）をタップして選択します
2. **工事名** のドロップダウンから対象の工事を選択します
3. 工事を選択すると、**工程名** のドロップダウンが有効になりますので、対象の工程を選択します
4. 両方を選択したら **「次へ」** ボタンをタップして、画像認識画面に進みます

### その他の機能

- **再読込ボタン**（🔄 アイコン）：サーバーから最新の工事・工程データを取得し直します
- **ログアウトボタン**（🚪 アイコン）：ログイン画面に戻ります
- **「履歴」ボタン**：過去の登録履歴を確認できます（詳細は [登録履歴の確認・同期](#8-登録履歴の確認同期) を参照）

---

## 4. 画像認識（撮影・ギャラリー選択）

工事・工程を選択して「次へ」をタップすると、**画像認識画面** に遷移します。ここでは、製品番号が写った画像を用意する方法を選択します。

### 操作手順

画像の取得方法は2つあります。

- **「カメラで撮影」ボタン**：カメラを起動して、その場で製品番号を撮影します（詳細は次のセクション参照）
- **「ギャラリーから選択」ボタン**：端末のギャラリーに保存済みの画像を選んで使用します

### 画像を取得した後

画像を取得すると、プレビューエリアに撮影した画像が表示されます。ここで以下の操作が可能です。

- **左90°** / **右90°** ボタン：画像の向きが正しくない場合に回転できます。製品番号が正しく読める向きに調整してください
- **「文字を認識する」ボタン**：画像に写っている製品番号のOCR認識を開始します

---

## 5. カメラで撮影する

「カメラで撮影」を選択すると、**カメラ画面** が起動します。

### 画面の見かた

- **中央のプレビュー**：カメラが映している映像がリアルタイムに表示されます。検出された文字領域が緑色や水色の枠線で囲まれて表示されます
- **四隅のコーナーマーク**：撮影の参考ガイドです。品質に問題がない場合は緑色、ぼけや傾きが検出された場合は赤色に変わります
- **品質警告メッセージ**：「ぼけています」「傾いています」といった警告が自動的に表示されます
- **上部のフラッシュアイコン**：タップするたびに、フラッシュの設定が「自動 → 常時ON → OFF」の順に切り替わります
- **下部の明るさスライダー**：暗い場所での撮影時に、明るさを手動で調整できます

### 撮影のコツ

- **ピンチ操作**（2本指で広げる / 縮める）でズームイン・ズームアウトができます
- **タップ** した場所にピントが合います。製品番号の部分をタップしてピントを合わせてから撮影すると、認識精度が向上します
- 製品番号がはっきり読める距離・角度で撮影してください
- 撮影ボタン（中央下部の青いカメラアイコン）をタップすると、プレビューに映っている映像全体がそのまま保存されます

### 撮影後

シャッターボタンを押すと、撮影された画像が自動的に前の画面（画像認識画面）に渡されます。

---

## 6. 認識結果の確認

画像認識画面で「文字を認識する」をタップすると、OCR処理が実行されます。処理が完了すると **認識結果確認画面** に遷移します。

### 画面の構成

- **上部の画像**：認識された文字領域がハイライト表示されたオーバーレイ画像が表示されます
- **登録先情報**：現在選択中の工事名・工程名が表示されます。「変更」ボタンで変更も可能です
- **認識結果リスト**：OCRで読み取られた製品コードが、クロップ画像（切り出された部分画像）と共に一覧表示されます

### 認識結果の操作

各製品コードの項目では、以下の操作が可能です。

- **チェックボックス**：登録する製品コードを選択・選択解除できます
- **製品コードの編集**：認識結果が誤っている場合、タップして正しいコードに修正できます
- **削除**：不要な認識結果を削除できます
- **「手動で追加」**：OCRで検出されなかった製品コードを手動で追加できます

### 参考表示

正解ラベルとの編集距離（文字のずれの度合い）が大きい認識結果は、黄色い背景の「参考」セクションに表示されます。これらは登録候補としては使用されませんが、確認用として参照できます。

### 次のステップ

- **「選択した製品を登録」ボタン**：チェックが入っている製品コードを登録処理に進めます
- **「再撮影」ボタン**：撮影からやり直します

---

## 7. 製品コードの登録

認識結果確認画面で「選択した製品を登録」をタップすると、**登録画面** に遷移します。

### 画面の構成

- **登録する製品コード一覧**：登録対象として選択された製品コードが表示されます
- **工事・工程情報**：登録先の工事名と工程名が表示されます。「変更」ボタンで修正可能です

### 登録の実行

画面下部の **「登録」** ボタンをタップすると、登録処理が実行されます。

- **オンライン時**：サーバーに直接登録データが送信されます。「登録が完了しました」と表示されます
- **オフライン時**：端末内部のデータベースに保存されます。「オフラインで保存しました。オンライン復帰時に自動送信されます。」と表示されます

---

## 8. 登録履歴の確認・同期

工事・工程選択画面の **「履歴」** ボタンをタップすると、**登録履歴画面** が表示されます。

### 履歴の確認

過去に登録した製品コードの一覧が時系列で表示されます。上部の検索バーに製品コードや工事名の一部を入力すると、履歴を絞り込むことができます。

### 同期ステータス

各登録データには同期ステータスが表示されます。

| ステータス | 意味 |
|---|---|
| **送信済み** | サーバーへの送信が完了しています |
| **未送信** | まだサーバーに送信されていません（オフライン登録分など） |
| **送信中** | 現在サーバーへの送信処理中です |
| **送信失敗** | 送信に失敗しました。再試行が必要です |

### 手動同期

画面上部に未送信データがある旨のバーが表示された場合、**「同期」** ボタンをタップすることで手動でサーバーとの同期を実行できます。なお、アプリはバックグラウンドでも15分間隔で自動的に同期を試みます。

---

## 9. 管理画面（サーバー側）

サーバーが起動している場合、PCのWebブラウザから管理画面にアクセスできます。

### アクセス方法

ブラウザで以下のURLを開いてください。

```
http://<サーバーのIPアドレス>:5000/admin
```

ローカルのPCでサーバーを起動している場合は `http://localhost:5000/admin` でアクセスできます。

### 管理画面でできること

- 登録データの一覧確認
- 製品ラベル（マスターデータ）の管理
- 工事・工程データの管理
- CSV形式でのデータエクスポート（`http://<サーバーIP>:5000/api/export/csv`）

---

## 10. よくある質問・トラブルシューティング

### Q. カメラが起動しない

**A.** アプリに対してカメラの使用が許可されているか確認してください。端末の「設定」→「アプリ」→「Cross Vision F」→「権限」からカメラの権限を許可できます。

### Q. 製品番号がうまく認識されない

**A.** 以下の点を確認してください。

- 画像がぼけていないか（カメラ画面で「ぼけています」の警告が出ていたら、ピントを合わせ直してください）
- 製品番号が画像内で十分な大きさで映っているか（ピンチ操作でズームインしてみてください）
- 画像が傾きすぎていないか（「傾いています」の警告が出ている場合は、端末を水平に構えてください）
- 暗すぎないか（明るさスライダーで調整するか、フラッシュをONにしてみてください）

### Q. 認識結果の製品コードが間違っている

**A.** 認識結果確認画面で製品コードをタップすると編集できます。正しい製品コードに修正してから登録してください。また、「手動で追加」ボタンから直接入力することもできます。

### Q. オフラインで登録したデータはどうなる？

**A.** 端末内部のデータベースに保存されています。Wi-Fiに接続してサーバーと通信できる状態になると、アプリが15分間隔で自動的にデータを送信します。すぐに同期したい場合は、登録履歴画面の「同期」ボタンをタップしてください。

### Q. サーバーに接続できない

**A.** 以下の点を確認してください。

- スマートフォンとサーバーPCが同じ Wi-Fi ネットワークに接続されていること
- サーバーがポート5000で起動していること（`http://<サーバーIP>:5000/health` にブラウザでアクセスして確認できます）
- サーバーPCのファイアウォールがポート5000をブロックしていないこと

### Q. アプリのデータを初期化したい

**A.** 端末の「設定」→「アプリ」→「Cross Vision F」→「ストレージ」→「データを削除」で、アプリ内に保存されている全データ（ログイン情報、登録履歴、キャッシュ等）を初期化できます。ただし、未送信のデータも削除されますのでご注意ください。

---

## アプリの画面遷移の流れ

```
ログイン → 工事・工程選択 → 画像認識 → カメラ撮影
                ↑                         ↓
              履歴    ← 登録 ← 認識結果確認
```

全体の基本的な流れは、**ログイン → 工事と工程を選ぶ → カメラで撮影 → 認識結果を確認・修正 → 登録** という手順になります。登録後は工事・工程選択画面に戻り、続けて次の撮影・登録作業を行うことができます。

---

*Cross Vision F — 鉄骨工程管理システム*
